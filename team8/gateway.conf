server {
  listen 80 default_server;
  server_name _;

  client_max_body_size 1024m;

  location = /healthz {
    add_header Content-Type text/plain;
    return 200 "ok\n";
  }

  # Backend (put this BEFORE /team8/ frontend)
  location /team8/api/ {
    proxy_pass http://backend:8002/api/;  # /team8/api/x -> /api/x
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # Optional convenience for your vite proxy (/api -> backend)
  location /api/ {
    proxy_pass http://backend:8002/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # Frontend
  location /team8/ {
    proxy_pass http://frontend:80;  # keep URI as-is
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }

  location = / {
    return 302 /team8/;
  }
}

